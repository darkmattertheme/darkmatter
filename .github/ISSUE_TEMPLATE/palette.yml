name: Palette
description: Propose a change to the core palette or the generated files.
labels: [palette]
body:
  - type: textarea
    attributes:
      label: What should change, and why?
      description: Every port copies the palette, so include the colors involved and where the problem shows up.
    validations:
      required: true
